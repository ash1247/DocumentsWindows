#include <stdio.h>
#include <stdlib.h>

int main( void )
{
    FILE *fPointer;

    /*fPointer = fopen("Anip.txt", "w");

    fprintf(fPointer, "I love her.\n");
    fprintf(fPointer, "I love her2.\n");
    fprintf(fPointer, "I want a harem.\n");

    fclose(fPointer); */


    /*fPointer = fopen("Anip.txt", "a");
    fprintf(fPointer, "With girls from every country.\n");*/

    /*fPointer = fopen("Anip.txt", "r");
    char singleLine[150];


    while( fgets( singleLine, 150, fPointer) != NULL )
    {
        printf("%s", singleLine);
    }

    fclose(fPointer);*/

    return 0;
}
