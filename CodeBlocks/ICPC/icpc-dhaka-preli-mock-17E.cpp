#include <bits/stdc++.h>

using namespace std;

int main( void )
{
    int t;
    cin >> t;

    for( int i = 1; i <= t; i++ )
    {
        int n, q;
        cin >> n;
        cin >> q;

        int A[n];
        int B[ q * 3 + 1];

        for( int j = 1; j <= q * 3; j++ )
        {
            cin >> B[j];
        }

    }
}
