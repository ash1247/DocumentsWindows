#include <bits/stdc++.h>

using namespace std;

int main( void )
{
    int n, p;
    cin >> n >> p;

    string s[n];

    for(int i = 0; i < n; i++ )
    {
        cin >> s[i];
    }

    cout << p << endl;
}
