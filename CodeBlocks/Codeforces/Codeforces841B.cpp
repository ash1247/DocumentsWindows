#include <bits/stdc++.h>

int main( void )
{
    int n;
    long long int i, j;
    long long int sumOdd, sumEven;

    scanf("%d", &n);

    std::vector<long long int> v;

    for(i = 1; i <= n; i++)
    {
        v.push_back(i);
    }

    for(i = 1; i <= n; i++)
    {
       // std::cout << v << '\n';
    }

    for(i = 1; i <= n; i++)
    {
        if( sumOdd % 2 == 1)
        {

        }

    }


}
