#include <bits/stdc++.h>

using namespace std;

int main(void)
{

    long long int n, mod = 0, check = 0;

    cin >> n;

    mod = n % 10;

    if( mod <= 5)
    {
        n -= mod;
    }
    else if( mod > 5)
    {
        check = 10 - mod;
        n += check;
    }

    cout << n << endl;
    /*string s;

    cin >> s;

    int x = '5' + 1;
    cout << x << endl;
    int l = s.length();

    if(s[l - 1] >= '0' & s[l - 1] <= '5')
    {
        s[l - 1] = '0';
    }
    else if(s[l - 1] >= '6' & s[l - 1] <= '9')
    {
        s[l - 1] = '0';
        s[l - 2] += 1;
    }

    cout << s << endl;*/

    return 0;
}
